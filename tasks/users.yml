---
- name: "Add jenkins to the shadow group (required for the PAM module)"
  user:
    name: "jenkins"
    groups: "shadow"
    generate_ssh_key: true
    append: true

# TODO: update admin jenkins login to use ssh key file
